@model AddNewPatientRecordViewModel
@{
    ViewData["Title"] = "Add New Record";
}
<!--
@section NavButtons {
    <a href="PatientLookUp" class="topnav-lbtn topnav-first">Patient Look-Up</a>
    <a href="PatientOverview" class="topnav-lbtn">Patient Overview</a>
    <a href="Logout" class="topnav-rbtn">Logout</a>
    <a href="patientLookUp" class="topnav-rbtn">@ViewBag.DoctorName</a>
}

<div class="add-record">
    <div class="form">
        <div class="heading">
            <p>Add New Patient Record</p>
        </div>
        <br />
        <form asp-action="AddNewPatientRecord" method="post">
            <div>
                <label for="selectRecordType">New Record Type</label>
                <select id="selectRecordType">
                    <option value="" disabled selected hidden>Select Record Type</option>
                    <option value="doctorsNote">Add New Doctor's Note</option>
                    <option value="prescription">Add New Prescription</option>
                    <!--<option value="testResult">Add New Test Results</option>-->
                </select>
            </div>

            <div id="doctorsNote1">
                <label>Purpose of Visit</label>
                @Html.TextBoxFor(n => n.DoctorsNote.PurposeOfVisit, new { placeholder = "Main concern addressed" })
            </div>

            <div id="doctorsNote2">
                <label>Description</label>
                @Html.TextAreaFor(n => n.DoctorsNote.Description, new { @rows = 3, placeholder = "Enter appointment notes" })
            </div>

            <div id="doctorsNote3">
                <label>Final Diagnoses</label>
                @Html.TextBoxFor(n => n.DoctorsNote.FinalDiagnosis, new { placeholder = "Identification of concern raised" })
            </div>

            <div id="doctorsNote4">
                <label>Further Instructions and General Precautions</label>
                @Html.TextAreaFor(n => n.DoctorsNote.FurtherInstructions, new { @rows = 3, placeholder = "Physical/dietary precautions, restrictions and recommendations" })
            </div>

            <div id="prescription1">
                <label>Prescribing Date</label>
                @Html.TextBoxFor(n => n.Prescription.PrescribingDate, new
                {
                    type = "text",
                    id = "todaysDate"
                })
            </div>

            <div id="prescription2">
                <label>Drug Name and Strength</label>
            </div>

            <div id="prescription3">
                <label>Dosage</label>

            </div>

            <div id="prescription4">
                <div class="row">
                    <div class="column">
                        <label>Start Day</label>
                    </div>
                    <div class="column">
                        <label>End Day</label>
                    </div>
                </div>
                <div class="row">
                    <div class="column">
                        @Html.TextBoxFor(n => n.Prescription.StartDate, new
                        {
                            type = "date",
                            min = "2020-04-01",
                            max = "2050-04-01"
                        })
                    </div>
                    <div class="column">
                        @Html.TextBoxFor(n => n.Prescription.EndDate, new
                        {
                            type = "date",
                            min = "2020-04-01",
                            max = "2050-04-01"
                        })
                    </div>
                </div>
            </div>

            <div id="prescription5">
                <label>Direction for Use or Special Instructions</label>
                @Html.TextAreaFor(n => n.Prescription.DirectionForUse, new { @rows = 3, placeholder = "Restrictions and Recommendations" })
            </div>

            <!--
            <div id="test1">
                <label for="selectTestType">Test Type</label>
                <select id="selectRecordType">
                    <option value="" disabled selected hidden>Select Test Type</option>
                    <option>Hematology</option>
                    <option>Urine Tests</option>
                    <option>Chemistry</option>
                    <option>Microbiology</option>
                    <option>Lipid</option>
                    <option>Thyroid Functions</option> 
                    <option>Other tests</option>
                    <option>Other chemistry tests</option>
                </select>
            </div>

            <div id="test2">
                <label>Reason for Test</label>
                <textarea rows="3" placeholder="List symptoms and suspected illness"></textarea>
            </div>

            <div id="test3">
                <label>Attach file</label>
                <input type="file">
                <label>Choose file</label>
            </div>
            -->
            <button type="submit" id="buttonSubmit">Submit</button>
        </form>
    </div>
</div>

<!-- Select2 -->
<script src="~/theme/plugins/select2/js/select2.full.min.js"></script>
<!-- Bootstrap4 Duallistbox -->
<script src="~/theme/plugins/bootstrap4-duallistbox/jquery.bootstrap-duallistbox.min.js"></script>
<!-- InputMask -->
<script src="~/theme/plugins/moment/moment.min.js"></script>
<script src="~/theme/plugins/inputmask/min/jquery.inputmask.bundle.min.js"></script>
<!-- date-range-picker -->
<script src="~/theme/plugins/daterangepicker/daterangepicker.js"></script>
<!-- bootstrap color picker -->
<script src="~/theme/plugins/bootstrap-colorpicker/js/bootstrap-colorpicker.min.js"></script>
<!-- Tempusdominus Bootstrap 4 -->
<script src="~/theme/plugins/tempusdominus-bootstrap-4/js/tempusdominus-bootstrap-4.min.js"></script>
<!-- Bootstrap Switch -->
<script src="~/theme/plugins/bootstrap-switch/js/bootstrap-switch.min.js"></script>
<!-- jQuery -->
<script src="~/theme/plugins/jquery/jquery.min.js"></script>
<!-- InputMask -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery.mask/1.14.15/jquery.mask.min.js"></script>
<!-- Bootstrap 4 -->
<script src="~/theme/plugins/bootstrap/js/bootstrap.bundle.min.js"></script>
<!-- bs-custom-file-input -->
<script src="~/theme/plugins/bs-custom-file-input/bs-custom-file-input.min.js"></script>
<!-- AdminLTE App -->
<script src="~/theme/dist/js/adminlte.min.js"></script>
<!-- AdminLTE for demo purposes -->
<script src="~/theme/dist/js/demo.js"></script>

<script type="text/javascript">
    $(document).ready(function () {

        bsCustomFileInput.init();
        var today = new Date();
        var month = (today.getMonth() + 1 < 10 ? '0' + (today.getMonth() + 1) : today.getMonth() + 1)
        var day = (today.getDate() + 1 < 10 ? '0' + (today.getDate() + 1) : today.getDate() + 1)
        var date = (today.getFullYear() + '-' + month + '-' + day);
        $('#todaysDate').val(date);

    });
    function isFloat(evt) {

        var charCode = (event.which) ? event.which : event.keyCode;
        if (charCode != 46 && charCode > 31 && (charCode < 48 || charCode > 57)) {
            //alert('Please enter only no or float value');
            return false;
        }
        else {
            //if dot sign entered more than once then don't allow to enter dot sign again. 46 is the code for dot sign
            var parts = evt.srcElement.value.split('.');
            if (parts.length > 1 && charCode == 46) {
                return false;
            }

            return true;

        }
    }

    //Datemask mm/dd/yyyy
    $('#todaysDate').mask('00/00/0000', { 'placeholder': 'mm/dd/yyyy' })
    $('#datemask2').mask('00/00/0000', { 'placeholder': 'mm/dd/yyyy' })
    $('#datemask3').mask('00/00/0000', { 'placeholder': 'mm/dd/yyyy' })

    $("#selectRecordType").change(function () {
        $('#buttonSubmit').hide();
        if ($(this).val() == "doctorsNote") {
            $('#doctorsNote1').show();
            $('#doctorsNote2').show();
            $('#doctorsNote3').show();
            $('#doctorsNote4').show();
            $('#buttonSubmit').show();
        } else {
            $('#doctorsNote1').hide();
            $('#doctorsNote2').hide();
            $('#doctorsNote3').hide();
            $('#doctorsNote4').hide();
        }

        if ($(this).val() == "prescription") {
            $('#prescription1').show();
            $('#prescription2').show();
            $('#prescription3').show();
            $('#prescription4').show();
            $('#prescription5').show();
            $('#buttonSubmit').show();
        } else {
            $('#prescription1').hide();
            $('#prescription2').hide();
            $('#prescription3').hide();
            $('#prescription4').hide();
            $('#prescription5').hide();
        }

        /*        if ($(this).val() == "testResult") {
                    $('#test1').show();
                    $('#test2').show();
                    $('#test3').show();
                    $('#submit').show();
                } else {
                    $('#test1').hide();
                    $('#test2').hide();
                    $('#test3').hide();
                }*/
    });
    $("#selectRecordType").trigger("change");</script>