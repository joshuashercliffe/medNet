
@{
    ViewData["Title"] = "PrescriptionList";
}

@section NavButtons {
    <a href="Logout" class="topnav-rbtn topnav-first">Logout</a>
    @*<a href="PatientOverview" class="topnav-rbtn">@Model.PatientName</a>*@
}

<body>
    <div class="sidebar">
        <div class="sidebar-body">
            <a href="PatientOverview">Overview</a>
            <a href="PreviousAppointments">Past Appointments</a>
            <a href="PrescriptionList" class="active">Prescriptions</a>
            <a href="TestResultList">Test Results</a>
        </div>
    </div>

    <div class="content">
        <h1>Active Prescriptions</h1>
        <!-- dynamically show the prescriptions with at least 1 refill left-->
        <div class="prescription">
            <div class="prescription header" id="headingOne">
                <button class="btn btn-link" data-toggle="collapse" data-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                    <h3>PrescriptionName1</h3>
                </button>
                <div class="row">

                </div>
                <p>Purpose: of prescription</p>
                <p>Refills: No. of refills left</p>
            </div>
            <div id="collapseOne" class="collapse" aria-labelledby="headingOne">
                <div class="prescription-body">
                    <p>Prescription details here</p>
                </div>
            </div>
        </div>
 

        <div class="prescription">
            <div class="prescription-header" id="headingTwo">
                <button class="btn btn-link" data-toggle="collapse" data-target="#collapseTwo" aria-expanded="true" aria-controls="collapseOne">
                    <h3>PrescriptionName2</h3>
                </button>
                <p>Purpose: of prescription</p>
                <p>Refills: No. of refills left</p>
            </div>
            <div id="collapseTwo" class="collapse" aria-labelledby="headingTwo">
                <div class="prescription-body">
                    <p>Prescription2 details here</p>
                </div>
            </div>
        </div>



            <h1>Past Prescriptions</h1>
            <!-- dynamically show all prescriptions with 0 refills left-->
            @*<div id="prescriptions" class="tabcontent">
            @if (Model != null && Model.Prescriptions != null && Model.Prescriptions.Any())
            {
                @foreach (var prescription in Model.Prescriptions)
                {
                    <div class="record" align="left">
                        <div class="heading1">
                            Date: @prescription.PrescribingDate.ToString("MMMM dd, yyyy")
                        </div>
                        <p>
                            Prescription: @prescription.DrugNameStrength <br />
                            Dosage: @prescription.Dosage <br />
                            Prescription Length: @prescription.StartDate.ToString("MMMM dd, yyyy") to @prescription.EndDate.ToString("MMMM dd, yyyy") <br />
                            Prescribing Doctor: @prescription.DoctorName
                        </p>
                    </div>
                }
            }
            else
            {
                <div class="record" align="left">
                    <div class="heading1">
                        No records shared.
                    </div>
                    <p>
                        No medical records to show.
                    </p>
                </div>
            }
        </div>*@
        </div>
</body>